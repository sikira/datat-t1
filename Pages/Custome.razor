@using DataTalksFrontendExercise.Components;
@using DataTalksFrontendExercise.Dto;
@using DataTalksFrontendExercise.Services;

@page "/page2"

@code {
    [Inject]
    private IBaseCardService baseCardService { get; set; } = null!;


}
@*This are test components that implement different use cases with custome class and style externaly added to component*@

<BaseCard BaseCardModel="baseCardService.GetLoremIpsumModelColor" HolderClass="color-full" />

<div class="notify-test">
    <BaseCard BaseCardModel="baseCardService.GetLoremIpsumModel" HolderStyle="opacity:.85;" />
</div>

<style>
    .color-full {
        background: #00b3b3 !important;
        border: 4px dashed #026b6b;
        color: #f2f4f5 !important;
    }

    .notify-test {
        position: absolute;
        bottom: 30px;
        right: 30px;
    }
</style>